---
about-csap-service-json:
- "Any field in service definition can be selectively overridden."
- "Alternately - the entire service definition may be placed  here to externalize\
  \ from Application.json"
alerts:
  max_diskUtil: "200g"
  max_rssMemory: "10g"
docker:
  limits:
    memoryInMb: 9000
